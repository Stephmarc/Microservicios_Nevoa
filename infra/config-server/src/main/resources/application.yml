server:
  port: 7071

spring:
  application:
    name: config-server
  profiles:
    active: native
  cloud:
    config:
      server:
        native:
          search-locations: ${CONFIG_REPO_LOCATION:file:./infra/config-repo}

management:
  endpoints:
    web:
      exposure:
        include: health,info
