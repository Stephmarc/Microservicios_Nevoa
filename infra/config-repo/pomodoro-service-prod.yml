server:
  port: 8087

spring:
  datasource:
    url: jdbc:mysql://${POMODORO_DB_HOST:mysql-pomodoro}:${POMODORO_DB_PORT:3306}/${POMODORO_DB_NAME:db_nevoa_pomodoro}?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
    username: ${POMODORO_DB_USERNAME:root}
    password: ${POMODORO_DB_PASSWORD:root}
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: false
    properties:
      hibernate:
        format_sql: true
  flyway:
    enabled: true
    locations: classpath:db/migration

eureka:
  client:
    service-url:
      defaultZone: ${EUREKA_SERVER_URL:http://registry-server:7081/eureka/}
  instance:
    prefer-ip-address: true

springdoc:
  api-docs:
    enabled: true
    path: /v3/api-docs
  swagger-ui:
    enabled: true
    path: /swagger-ui.html

management:
  endpoints:
    web:
      exposure:
        include: health,info
